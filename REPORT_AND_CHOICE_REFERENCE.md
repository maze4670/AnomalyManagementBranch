# REPORT_AND_CHOICE_REFERENCE.md

## 1. 문서 목적

이 문서는 정식 이상현상의 보고와 대응 선택지 문서를 작성하기 위한 실무 레퍼런스다.

이 문서는 `CASE_DESIGN_RULES.md`와 `ANOMALY_CONTENT_REFERENCE.md` 아래에 위치한다.

`ANOMALY_CONTENT_REFERENCE.md`가 원본 설계 문서와 게임용 이상현상 문서 추출 기준을 다룬다면, 이 문서는 원본 설계 문서를 실제 게임 진행 구조로 분해하는 기준을 다룬다.

이 문서에서 다루는 대상은 다음이다.

- 보고 노드
- 대응 선택지
- 후속 보고 연결
- 안정화 노드
- 격리 실패 노드
- 처리 지연 구조
- 안정화 이후 특수 이벤트 루트
- 진행 상황 / 기록 보관실에 남는 보고와 대응 기록

이 문서는 새 이상현상 내용을 창작하는 문서가 아니다.

이 문서는 사용자가 작성하거나 승인한 원본 설계 문서를 바탕으로, 보고와 대응 선택지 구조를 정리하기 위한 문서다.

---

## 2. 보고와 대응 선택지 문서의 역할

보고와 대응 선택지 문서는 개발용 내부 문서다.

플레이어에게 직접 표시되는 문서가 아니다.

이 문서는 게임 안에서 다음 역할을 한다.

- 보고 루트 그래프 구성
- 보고 노드 관리
- 선택지 분기 관리
- 선택지별 다음 보고 연결
- 선택지별 내부 상태 변화 관리
- 후속 보고 예약
- 안정화 노드 관리
- 격리 실패 노드 관리
- 안정화 이후 특수 이벤트 관리
- 진행 상황 / 기록 보관실에 표시될 보고와 대응 기록의 원천 제공

플레이어에게 보여주는 것은 보고 내용과 선택지 문장뿐이다.

플레이어에게 내부 노드 ID, 선택지 ID, 상태 수치, 지연 일수, 계산식은 표시하지 않는다.

---

## 3. 원본 설계 문서와의 관계

보고와 대응 선택지 문서는 반드시 원본 설계 문서를 기준으로 작성한다.

원본 설계 문서 없이 보고 노드나 선택지를 먼저 만들지 않는다.

원본 설계 문서에서 가져올 정보는 다음이다.

- 최초 보고로 사용할 수 있는 사건
- 후속 보고로 사용할 수 있는 사건
- 기관 오판이 드러나는 기록
- 플레이어가 판단해야 하는 containment 방향
- Containment Truth
- 안정화 방향
- 격리 실패 방향
- contamination 또는 확산 가능성
- 안정화 이후 특수 이벤트 후보
- 기록 보관실에 남길 수 있는 보고와 대응 기록

Codex나 AI는 원본 설계 문서에 없는 보고 사건, 선택지, 특수 이벤트를 임의로 만들 수 없다.

부족한 정보가 있으면 사용자에게 질문해야 한다.

---

## 4. 보고 루트 기본 구조

각 이상현상은 보고 루트 그래프를 가진다.

기본 구조는 다음과 같다.

```text
보고 노드
→ 대응 선택지
→ 다음 보고 노드
→ 안정화 또는 격리 실패
```

보고 루트는 단순한 직선 구조가 아니다.

선택에 따라 분기될 수 있고, 이후 다른 보고 노드로 합류할 수 있다.

단, 모든 분기와 합류는 원본 설계 문서의 행동 원리와 containment 판단에 근거해야 한다.

보고 루트는 자동 생성하지 않는다.

모든 보고 노드, 선택지, 다음 노드, 안정화 노드, 격리 실패 노드는 사전에 설계한다.

---

## 5. 일반 보고 루트 깊이

일반 보고 루트의 최대 깊이는 5층이다.

여기서 깊이는 보고 노드가 선택지를 거쳐 다음 보고로 이어지는 단계를 의미한다.

예시:

```text
1층: 최초 보고
2층: 1차 대응 결과 보고
3층: 2차 대응 결과 보고
4층: 심화 대응 결과 보고
5층: 안정화 또는 격리 실패 직전 보고
```

일반 보고 루트는 무한히 이어지지 않는다.

보고 루트의 끝은 다음 중 하나다.

- 단 하나의 안정화 노드
- 여러 개의 격리 실패 노드

---

## 6. 보고 노드 기본 구조

각 보고 노드는 하나의 상황 판단 단위다.

보고 노드는 다음 정보를 가진다.

```text
보고 노드 ID
보고 내용
대응 선택지 목록
```

보고 노드의 역할은 다음이다.

- 현재 이상현상 상태를 보여준다.
- 플레이어가 판단해야 할 단서를 제공한다.
- 이전 대응의 결과를 간접적으로 보여준다.
- 다음 대응 선택의 기준을 제공한다.

보고 내용은 정답 해설이 아니다.

보고 내용은 플레이어가 상황을 읽고 containment 방향을 추론할 수 있도록 작성한다.

---

## 7. 보고 노드 작성 기준

보고 내용에는 다음이 포함될 수 있다.

- 현장 보고
- 조사 기록
- 민간 신고 요약
- CCTV 기록 요약
- 내부 메일 요약
- 증언
- 행정 기록
- contamination 관찰 기록
- 이상현상 반응 변화

보고 내용은 다음을 직접 말하면 안 된다.

- 정답 선택지
- 실패 선택지
- 상태 수치 변화
- 기관 신뢰도 변화
- 후속 보고 지연 일수
- 성공률
- 실패 확률
- 내부 계산식
- 노드 ID
- 선택지 ID

나쁜 보고 방향:

```text
이전 선택은 실패였습니다.
상태 수치가 -2 감소했습니다.
다음에는 A 선택지를 골라야 합니다.
```

좋은 보고 방향:

```text
현장 기록, 반복 표현, 행정 이상, 증언 변화, 대상자의 행동 변화 등을 통해 결과를 간접적으로 보여준다.
```

---

## 8. 보고 라벨 작성 기준

보고 라벨은 보고 노드에 고정 저장하는 문장이 아니라, 해당 보고가 현재 회차에서 확인된 일차를 기준으로 표시되는 UI 라벨이다.

진행 상황과 기록 보관실에서는 아래 형식을 사용한다.

```text
X일차 보고
```

예:

```text
3일차 보고
```

주의:

- `X일차 보고`는 보고 노드 JSON에 고정 문장으로 박아두는 값이 아니다.
- 실제 표시 일차는 플레이 중 해당 보고가 도착하거나 확인된 회차 일차를 기준으로 정한다.
- 보고 노드 자체는 내부적으로 `node_id`로 관리한다.
- 플레이어에게 `node_id`는 표시하지 않는다.
- 구현상 임시 필드가 필요하더라도 최종 UI 표시는 회차 일차 기반이어야 한다.

진행 상황과 기록 보관실에서는 일차별 접기/펼치기 구조를 사용한다.

접힌 상태:

```text
X일차 보고
▶
```

펼친 상태:

```text
X일차 보고
▼

보고 내용
선택한 대응 문장
```

진행 상황에는 보고 내용과 플레이어가 선택한 대응 문장을 함께 표시한다.

표시하지 않는 정보:

- 선택지 효과량
- 상태 수치 변화
- 기관 신뢰도 변화
- 성공률
- 후속 보고 지연 일수
- 내부 노드 ID

---

## 9. 대응 선택지 기본 구조

각 대응 선택지는 플레이어가 선택하는 containment 판단이다.

대응 선택지는 단순 버튼이 아니다.

대응 선택지는 다음을 포함한다.

```text
선택지 ID
선택지 문장
다음 노드 ID
상태 수치 변화
후속 보고 지연 범위
```

플레이어에게 표시되는 것은 선택지 문장뿐이다.

내부용 정보:

- choice_id
- next_node_id
- state_delta
- delay_range

위 정보는 플레이어 UI에 표시하지 않는다.

---

## 10. 대응 선택지 작성 기준

대응 선택지는 기관이 실제로 수행할 법한 containment 판단처럼 보여야 한다.

대응 선택지는 다음과 연결되어야 한다.

- 현재 보고 내용
- 이상현상의 행동 원리
- 기관의 초기 오판
- Containment Truth
- 이전 대응 결과
- 안정화 방향
- 격리 실패 방향

대응 선택지는 다음이 아니어야 한다.

- 정답 버튼
- 오답 버튼
- 의미 없는 조사 버튼
- generic system action
- 단순한 호감도 선택지
- 내부 수치 선택지

좋은 선택지는 플레이어가 읽었을 때 “이 기관이라면 실제로 이런 조치를 검토할 수 있겠다”고 느껴야 한다.

---

## 11. 선택지 개수 규칙

각 보고 노드는 기본적으로 2~3개의 대응 선택지를 가진다.

4개 이상의 선택지는 사용하지 않는다.

선택지가 후속 보고와 다음 선택지로 이어지는 구조이기 때문에, 한 노드의 선택지 수를 과도하게 늘리면 전체 보고 루트가 감당할 수 없게 커진다.

기본 원칙:

```text
2개:
명확한 두 containment 방향 중 하나를 선택해야 하는 경우

3개:
올바른 판단, 그럴듯한 오판, 보류 또는 우회적 판단이 구분되는 경우
```

선택지를 억지로 늘리기 위해 의미 없는 대응을 추가하지 않는다.

선택지 수보다 중요한 것은 각 선택지가 이상현상의 행동 원리와 연결된 containment 판단이어야 한다는 점이다.

---

## 12. 선택지 방향 설계

선택지 문장을 쓰기 전에 먼저 선택지 방향을 정리한다.

각 보고 노드에서 선택지는 다음 역할 중 하나를 가질 수 있다.

```text
안정화 방향
오판 방향
우회 또는 보류 방향
격리 실패 방향
```

단, 이 역할을 플레이어에게 직접 표시하지 않는다.

예시 형식:

```text
보고 노드:
- 안정화 방향:
- 오판 방향:
- 우회 / 보류 방향:
```

선택지 방향은 원본 설계 문서의 Containment Truth와 기관 오판을 기준으로 정한다.

---

## 13. choice_text 작성 기준

`choice_text`는 플레이어에게 실제로 보이는 대응 문장이다.

작성 기준:

- 기관 문서식 명령 또는 대응 지시처럼 작성한다.
- 선택 효과를 직접 설명하지 않는다.
- 정답/오답 느낌을 주지 않는다.
- 수치나 확률을 포함하지 않는다.
- 세계관에 없는 기능을 암시하지 않는다.
- 실제로 수행 가능한 containment 판단처럼 보이게 한다.

금지 예시:

```text
정답 대응을 수행한다.
상태 수치를 회복시킨다.
실패 확률을 낮춘다.
신뢰도를 올린다.
```

좋은 방향:

```text
현장 접근 절차를 제한하고 대상자의 이동 상태를 관찰한다.
해당 구조물을 즉시 철거하지 않고 출현 위치를 기록한다.
대상자에게 직접적인 위치 교정을 중단한다.
```

위 문장은 형식 예시이며, 정식 콘텐츠로 확정하지 않는다.

---

## 14. choice_id 작성 기준

`choice_id`는 내부 식별자다.

플레이어에게 표시하지 않는다.

`choice_id`는 다른 보고 노드나 다른 이상현상의 선택지와 혼동되지 않도록 작성한다.

권장 형식:

```text
report_001_choice_a
report_001_choice_b
report_001_choice_c
```

특수 이벤트의 경우:

```text
special_001_report_001_choice_a
special_001_report_001_choice_b
```

규칙:

- 같은 보고 노드 안에서 중복되지 않아야 한다.
- 가능하면 `node_id`를 포함해 어느 보고 노드의 선택지인지 알 수 있게 한다.
- `choice_a`처럼 너무 짧은 ID는 초안 단계에서만 사용할 수 있다.
- 정식 데이터 변환 전에는 `node_id` 포함 형식으로 정리한다.
- 플레이어 문서에는 표시하지 않는다.

주의:

- 실제 저장이나 완료 처리에서는 `case_id`, `node_id`, `choice_id`를 함께 사용해야 한다.
- `choice_id`만으로 전역 고유성을 가정하지 않는다.
- 다른 이상현상과 같은 `choice_id`가 있어도 `case_id`, `node_id`와 함께 관리되면 내부 충돌을 피할 수 있지만, 문서 제작 단계에서는 혼동을 줄이기 위해 `node_id` 포함 형식을 사용한다.

---

## 15. next_node_id 작성 기준

`next_node_id`는 선택 후 이어질 다음 보고 노드다.

가능한 연결 대상:

- 일반 보고 노드
- 안정화 노드
- 격리 실패 노드
- 특수 이벤트 루트 내부 보고 노드

일반 보고 루트에서는 다음 구조를 따른다.

```text
보고 노드
→ 선택지
→ 다음 보고 노드
```

루트 끝에서는 다음 중 하나로 연결된다.

```text
안정화 노드
격리 실패 노드
```

주의:

- next_node_id는 플레이어에게 표시하지 않는다.
- next_node_id는 내부 연결용이다.
- 연결할 노드가 없는데 임의로 새 노드를 만들지 않는다.
- 노드가 부족하면 원본 설계 문서를 다시 검토한다.

---

## 16. state_delta 작성 기준

`state_delta`는 선택지가 해당 이상현상의 내부 상태에 주는 변화량이다.

이 값은 플레이어에게 표시하지 않는다.

`state_delta`는 정식 밸런스 수치이므로 사용자가 승인해야 한다.

Codex나 AI가 임의로 확정하면 안 된다.

작성 기준:

- 안정화 방향은 상태를 유지하거나 개선하는 방향일 수 있다.
- 오판 방향은 상태를 악화시키는 방향일 수 있다.
- 치명적 오판은 격리 실패 노드로 직접 이어질 수 있다.
- 수치는 문서 작성 단계에서 임시로 두지 않는다.
- 수치가 미정이면 `미정`으로 남기고 사용자 확인을 받는다.

주의:

- state_delta는 플레이어에게 표시하지 않는다.
- 선택지 문장 안에 효과량을 쓰지 않는다.
- 상태 수치 변화는 보고 내용으로 직접 해설하지 않는다.

---

## 17. delay_range 작성 기준

`delay_range`는 선택지를 확정한 뒤 후속 보고가 도착하기까지의 랜덤 지연 일수 범위다.

선택지마다 `delay_range` 범위는 고정된다.

실제 후속 보고 지연 일수는 해당 범위 안에서 랜덤으로 결정된다.

예시:

```text
delay_range: [1, 3]
```

의미:

```text
후속 보고는 1~3일 사이에서 랜덤으로 예약된다.
```

후속 보고 지연 카운트는 선택지를 확정한 뒤에만 시작된다.

보고서를 처리하지 않은 상태에서는 후속 보고 카운트가 시작되지 않는다.

주의:

- delay_range는 플레이어에게 표시하지 않는다.
- 후속 보고 지연 일수는 UI에 표시하지 않는다.
- 선택지 문장으로 “며칠 뒤 보고”를 직접 설명하지 않는다.
- delay_range는 정식 밸런스이므로 사용자가 승인해야 한다.
- `delay_range`는 후속 보고 예약용 값이지, 처리 지연 악영향 수치가 아니다.

---

## 18. 처리 지연 구조

처리 지연은 보고서를 처리하지 않고 하루를 넘겼을 때 발생하는 내부 악화 구조다.

처리 지연은 별도의 표면 변화, 추가 보고, 경고 연출을 만들기 위한 장치가 아니다.

처리 지연의 핵심 의도는 플레이어가 처음에는 다음처럼 느끼게 만드는 것이다.

```text
보고서를 바로 처리하지 않아도 [처리 지연]이라고만 표시되고,
겉으로는 큰 문제가 없어 보인다.
```

하지만 실제로는 해당 이상현상의 내부 상태 수치가 계속 악화된다.

처리 지연 규칙은 아래 고정 구조를 따른다.

| 처리 지연 경과 | 상태 수치 악영향 |
|---|---:|
| 1일차 | -1 |
| 2일차 | -2 |
| 3일차 | -3 |
| 4일차 | -4 |

악영향은 누적 적용된다.

예시:

```text
1일 방치: -1
2일 방치: -1 + -2 = -3
3일 방치: -1 + -2 + -3 = -6
```

처리 지연 중에는 해당 이상현상의 루트 진행이 멈춘다.

후속 보고 지연 카운트도 보고서를 처리한 뒤에만 시작된다.

따라서 처리하지 않은 보고서는 다음 날로 이월되며, 플레이어 UI에는 `[처리 지연]` 라벨만 표시한다.

플레이어에게 표시하지 않는 정보:

- 미처리 일수
- 상태 수치 악영향
- 기관 신뢰도 변화
- 처리 지연 계산식
- 후속 보고 지연 일수

주의:

- 처리 지연 1일차, 2일차, 3일차, 4일차별 표면 변화를 만들지 않는다.
- 처리 지연 전용 보고를 만들지 않는다.
- 처리 지연으로 격리 실패 전조를 직접 표시하지 않는다.
- 처리 지연 중 플레이어에게 보여줄 보고 변화를 따로 만들지 않는다.
- UI에는 `[처리 지연]` 라벨만 표시한다.
- 플레이어에게 내부 악화를 설명하지 않는다.

---

## 19. 활성 보고서 제한

각 이상현상은 동시에 하나의 활성 보고서만 가질 수 있다.

보고서를 처리하지 않으면 해당 이상현상의 루트 진행은 멈춘다.

다른 이상현상의 보고서는 계속 진행될 수 있다.

이 규칙은 보고 루트 폭을 관리하기 위한 핵심 구조다.

한 이상현상에 동시에 여러 보고서를 열지 않는다.

---

## 20. 후속 보고 예약 구조

대응 확정 후 후속 보고는 즉시 표시되지 않는다.

후속 보고는 하루 종료 시 예약된다.

생성된 보고서는 다음 날 브리핑에 표시된다.

기본 흐름:

```text
보고서 확인
→ 대응 선택
→ 대응 확정
→ 하루 종료
→ 후속 보고 예약
→ 다음 날 브리핑 표시
→ 근무 화면에서 상세 확인
```

주의:

- 대응 선택지만 누른 상태에서는 확정되지 않는다.
- 대응 확정 버튼을 눌러야 하루 행동을 1회 소모한다.
- 후속 보고는 대응 확정 후에만 예약된다.
- 처리하지 않은 보고서는 후속 보고 예약이 시작되지 않는다.

---

## 21. 안정화 노드 규칙

안정화 노드는 일반 보고 루트의 종료 지점이다.

안정화는 이상현상의 완전 제거를 의미하지 않는다.

안정화는 보고와 선택지 노드를 무한히 만들 수 없기 때문에 설정하는 일반 루트의 끝이다.

각 이상현상은 단 하나의 안정화 노드를 가진다.

안정화 노드에 도달하면 해당 이상현상은 일반 보고 루트에서는 안정화된 것으로 처리된다.

그러나 안정화 이후에도 이상현상은 완전히 사라지지 않는다.

안정화 이후에는 정해진 확률 규칙에 따라 특수 이벤트가 발생할 수 있다.

안정화 노드 작성 기준:

- Containment Truth에 가까운 대응 흐름의 결과여야 한다.
- 단순히 “성공했습니다”라고 쓰지 않는다.
- 이상현상이 관리 가능한 상태에 들어갔음을 문서적으로 보여준다.
- 완전 제거를 암시하지 않는다.
- 이후 특수 이벤트 발생 여지를 남긴다.

---

## 22. 격리 실패 노드 규칙

격리 실패 노드는 해당 이상현상이 더 이상 관리 가능한 상태가 아니게 된 지점이다.

격리 실패 노드에 도달하면 보고 확인 즉시 게임 BAD 엔딩으로 이어진다.

격리 실패 노드는 여러 개 존재할 수 있다.

격리 실패 루트는 최소 3층에서 최대 5층으로 구성된다.

격리 실패는 다음에서 발생할 수 있다.

- 치명적 containment 오판
- 반복된 잘못된 대응
- 원본 설계 문서에서 정한 비가역 전환
- 이상현상의 행동 원리를 정반대로 이해한 대응
- 특정 후속 보고에서 드러나는 실패 조건 도달

격리 실패는 억까가 아니어야 한다.

플레이어가 보고서와 이전 결과를 통해 위험을 추론할 수 있어야 한다.

격리 실패 노드도 내부 수치나 정답 해설을 표시하지 않는다.

---

## 23. 일반 보고 루트의 분기와 합류

보고 루트는 단순 트리가 아니라 그래프 구조일 수 있다.

선택에 따라 분기될 수 있고, 이후 같은 보고 노드로 합류할 수 있다.

합류가 가능하더라도 이전 선택의 결과는 내부 상태나 기록에 남을 수 있다.

분기 설계 질문:

```text
이 선택은 어떤 containment 이해를 반영하는가?
이 선택은 다음 보고를 어떻게 바꾸는가?
이 선택은 안정화 방향인가, 오판 방향인가?
이 선택은 격리 실패 가능성을 높이는가?
```

합류 설계 질문:

```text
서로 다른 선택이 같은 보고로 합류해도 되는가?
합류해도 내부 상태 차이가 남는가?
합류 후 플레이어가 이전 선택의 결과를 간접적으로 읽을 수 있는가?
합류하면 안 되는 비가역 분기는 무엇인가?
```

---

## 24. 안정화 이후 특수 이벤트 구조

특수 이벤트는 일반 보고 루트 안에서 임의로 발생하는 사건이 아니다.

특수 이벤트는 이상현상이 안정화 노드에 도달한 뒤에만 발생할 수 있다.

안정화 노드에 도달하지 않은 이상현상은 특수 이벤트가 발생하지 않는다.

특수 이벤트의 역할은 이상현상이 완전히 제거되지 않았음을 보여주고, 안정화 이후에도 다시 관리 대상이 될 수 있도록 하는 것이다.

특수 이벤트 발생 확률은 안정화 후 경과일에 따라 아래 규칙을 따른다.

| 안정화 후 경과일 | 특수 이벤트 확률 |
|---|---:|
| 0~2일차 | 0% |
| 3일차 | 10% |
| 4일차 | 20% |
| 5일차 | 30% |
| 6일차 | 50% |
| 7일차 | 70% |
| 8일차 | 100% |

특수 이벤트 수:

```text
이상현상별 2개
```

특수 이벤트 루트:

```text
보고와 대응 선택지 구조는 일반 루트와 같다.
단, 최대 3층으로 구성된다.
```

표시 시점:

```text
특수 이벤트가 발생하면 다음 날 브리핑에 표시된다.
```

---

## 25. 특수 이벤트 보고 작성 기준

특수 이벤트 보고는 안정화 이후 다시 나타난 이상 징후다.

특수 이벤트 보고는 다음 조건을 만족해야 한다.

- 해당 이상현상의 행동 원리와 연결된다.
- 안정화가 완전 제거가 아니었음을 보여준다.
- 플레이어가 다시 대응해야 할 이유를 제공한다.
- 일반 보고 루트보다 짧다.
- 최대 3층 안에서 끝난다.
- 안정화로 되돌아가거나 격리 실패로 이어질 수 있다.

특수 이벤트는 다음이 아니어야 한다.

- 분위기용 랜덤 사건
- 점프 스케어
- 사례 원리와 무관한 돌발 상황
- 보상 이벤트
- 시스템 안내문

Codex나 AI는 특수 이벤트를 임의로 만들 수 없다.

특수 이벤트 내용은 원본 설계 문서에 있거나 사용자가 승인해야 한다.

---

## 26. additional_descriptions / 진행 상황 기록 규칙

`additional_descriptions`는 단순 보조 설명 목록이 아니다.

`additional_descriptions`는 이상현상에 대한 대응과 그에 따른 결과들의 모음이다.

플레이 중 보고서로 얻은 정보를 통해 해금된다.

현재 회차에서는 보고서 상세의 `진행 상황` 영역에 표시될 수 있고, 기록 보관실에서는 열람 가능한 보고와 대응 기록으로 표시될 수 있다.

진행 상황 표시 방식은 일차별 접기/펼치기 구조를 따른다.

접힌 상태:

```text
X일차 보고
▶
```

펼친 상태:

```text
X일차 보고
▼

보고 내용
선택한 대응 문장
```

작성 기준:

- 각 항목은 실제 보고 노드와 플레이어가 선택한 대응을 바탕으로 만들어진다.
- 임의의 보조 설명문을 추가하지 않는다.
- 보고 내용과 대응 문장을 통해 플레이어가 이전 판단을 되짚을 수 있게 한다.
- 기록 보관실에서도 시스템 해금 상태가 아니라 열람 가능한 기록만 보여준다.

표시하지 않는 정보:

- 선택지 효과량
- 상태 수치 변화
- 기관 신뢰도 변화
- 성공률
- 후속 보고 지연 일수
- 내부 노드 ID
- choice_id
- next_node_id
- state_delta
- delay_range
- unlock_level
- unlocked_report_keys
- 해금됨
- 일부 기록
- 전체 기록
- 갱신됨
- 해금 상태

---

## 27. 기록 보관실 반영 기준

기록 보관실에는 플레이어가 열람 가능한 보고와 대응 기록만 반영한다.

기록 보관실은 해금 상태를 설명하는 화면이 아니다.

기록 보관실에 표시하지 않는 문구:

- 해금됨
- 일부 기록
- 전체 기록
- 갱신됨
- 해금 상태
- 회차 종료 후 기록이 보관된다는 설명
- 기록이 추가되었다는 설명

BAD 엔딩 시에는 해당 회차에서 얻은 정보 중 일부만 보고 단위로 반영될 수 있다.

GOOD 엔딩 시에는 해당 회차에서 본 보고/대응 정보가 반영될 수 있다.

단, UI에는 partial, full, unlock_level, unlocked_report_keys 같은 내부 상태를 표시하지 않는다.

---

## 28. 내부 정보 비공개 규칙

보고와 대응 선택지 문서에는 내부 처리용 정보가 포함된다.

하지만 아래 정보는 플레이어 UI에 표시하지 않는다.

```text
node_id
choice_id
next_node_id
state_delta
delay_range
delay_days
anomaly_states
trust_value
applied_delay_penalties
unlock_level
unlocked_report_keys
partial
full
확률
상태 수치
기관 신뢰도 수치
선택지 효과량
내부 계산식
```

플레이어는 수치가 아니라 보고와 대응 결과를 통해 상태를 추론해야 한다.

---

## 29. 보고와 선택지 문서 작성 절차

보고와 선택지 문서는 아래 순서로 작성한다.

```text
1. 원본 설계 문서 확인
2. 최초 보고 후보 선택
3. 일반 보고 루트의 주요 단계 정리
4. 안정화 노드 정의
5. 격리 실패 노드 정의
6. 각 보고 노드의 대응 선택지 방향 정리
7. 선택지 문장 작성
8. next_node_id 연결
9. state_delta 초안 작성 또는 미정 표시
10. delay_range 초안 작성 또는 미정 표시
11. 진행 상황 / 기록 보관실에 남길 보고와 대응 기록 정리
12. 안정화 이후 특수 이벤트 2개 설계
13. 특수 이벤트 루트 작성
14. 사용자 검토
15. JSON 변환
```

주의:

- 원본 설계 문서에 없는 내용을 임의로 추가하지 않는다.
- 수치가 미정이면 임의로 확정하지 않는다.
- 선택지 문장이 부족하면 사용자에게 확인한다.
- Codex나 AI가 보고 노드를 자동 생성하지 않는다.

---

## 30. 일반 보고 노드 작성 템플릿

```markdown
## 보고 노드

node_id:

보고 목적:
- 이 보고가 플레이어에게 제공하는 판단 상황:

보고 내용:
"""

"""

플레이어가 추론해야 할 것:
- 

숨겨야 할 정보:
- 

대응 선택지:

### 선택지 1

choice_id:
choice_text:
선택지 방향:
next_node_id:
state_delta:
delay_range:

선택 결과 의도:
- 

기록에 남길 대응 문장:
- 

### 선택지 2

choice_id:
choice_text:
선택지 방향:
next_node_id:
state_delta:
delay_range:

선택 결과 의도:
- 

기록에 남길 대응 문장:
- 

### 선택지 3

choice_id:
choice_text:
선택지 방향:
next_node_id:
state_delta:
delay_range:

선택 결과 의도:
- 

기록에 남길 대응 문장:
- 
```

선택지가 2개인 보고 노드는 선택지 3 항목을 사용하지 않는다.

---

## 31. 안정화 노드 작성 템플릿

```markdown
## 안정화 노드

node_id:

안정화에 도달한 이유:
- 

보고 내용:
"""

"""

플레이어가 확인할 수 있는 변화:
- 

완전 제거로 오해되지 않게 남길 여지:
- 

기록에 남길 보고 내용:
- 
```

안정화 노드는 단 하나만 작성한다.

안정화는 완전 제거가 아니다.

---

## 32. 격리 실패 노드 작성 템플릿

```markdown
## 격리 실패 노드

node_id:

격리 실패에 도달한 이유:
- 

보고 내용:
"""

"""

플레이어가 이전 보고에서 추론할 수 있었던 전조:
- 

BAD 엔딩으로 이어지는 이유:
- 

기록에 남길 보고 내용:
- 
```

격리 실패 노드는 여러 개 있을 수 있다.

격리 실패 노드에 도달하면 보고 확인 즉시 BAD 엔딩으로 이어질 수 있다.

---

## 33. 특수 이벤트 루트 작성 템플릿

```markdown
## 특수 이벤트

event_id:

특수 이벤트 이름 또는 내부 구분명:

발생 조건:
- 안정화 후 특수 이벤트 판정에 따라 발생

첫 보고 node_id:

특수 이벤트 목적:
- 안정화 이후에도 이상현상이 완전 제거되지 않았음을 보여준다.

### 특수 이벤트 보고 노드 1

node_id:

보고 내용:
"""

"""

대응 선택지:
- 선택지 1
- 선택지 2
- 선택지 3

다음 노드 연결:
- 

### 특수 이벤트 후속 보고 노드

node_id:

보고 내용:
"""

"""

결말:
- 안정화 복귀
- 격리 실패
```

특수 이벤트 루트는 최대 3층으로 제한한다.

이상현상별 특수 이벤트는 2개를 기준으로 한다.

---

## 34. 보고 루트 전체 구조 템플릿

````markdown
# [보고와 대응 선택지 문서]

case_id:
display_id:
alias:

## 1. 일반 보고 루트 요약

최초 보고 node_id:

안정화 node_id:

격리 실패 node_id 목록:
- 
- 
- 

루트 최대 깊이:
- 최대 5층

## 2. 노드 연결 요약

```text
report_001
├─ report_001_choice_a → report_002
├─ report_001_choice_b → report_003
└─ report_001_choice_c → failure_001

report_002
├─ report_002_choice_a → stable_001
└─ report_002_choice_b → report_004
```

## 3. 보고 노드 목록

### report_001
...

### report_002
...

## 4. 안정화 노드

### stable_001
...

## 5. 격리 실패 노드

### failure_001
...

## 6. 특수 이벤트 루트

### special_event_001
...

### special_event_002
...

## 7. 진행 상황 / 기록 보관실 반영 후보

- X일차 보고:
  - 보고 내용:
  - 선택한 대응 문장:
````

---

## 35. 작성 완료 전 검토 질문

보고와 선택지 문서 작성 후 아래를 확인한다.

```text
모든 보고 노드가 원본 설계 문서에 근거하는가?
각 보고 노드가 판단 단서를 제공하는가?
각 보고 노드의 선택지가 2~3개인가?
4개 이상의 선택지를 만들지 않았는가?
각 선택지가 containment 판단처럼 보이는가?
선택지 문장이 정답/오답 버튼처럼 보이지 않는가?
choice_id가 node_id 포함 형식으로 정리되어 있는가?
next_node_id 연결이 모두 존재하는가?
delay_range가 범위 형식이며 실제 지연 일수는 범위 내 랜덤 적용임을 전제로 하는가?
일반 보고 루트가 최대 5층을 넘지 않는가?
안정화 노드가 단 하나인가?
격리 실패 노드가 하나 이상 존재하는가?
격리 실패 루트가 최소 3층 ~ 최대 5층인가?
처리 지연 전용 보고를 만들지 않았는가?
처리 지연 표면 변화를 만들지 않았는가?
안정화 이후 특수 이벤트가 2개 준비되어 있는가?
특수 이벤트 루트가 최대 3층을 넘지 않는가?
additional_descriptions가 보고와 대응 기록 기준으로 구성되는가?
내부 수치와 내부 ID가 UI에 노출되지 않는가?
Codex나 AI가 임의로 만든 보고나 선택지가 없는가?
```

검토가 끝나기 전에는 JSON 변환 단계로 넘어가지 않는다.
