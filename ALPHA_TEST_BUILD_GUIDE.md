# 외부 알파 테스트 빌드 준비 가이드

## 1. 목적과 범위

이 문서는 `AnomalyManagementBranch`의 제한된 외부 알파 테스트용 Windows 빌드를 준비하고 전달하기 위한 절차를 정리한다.

이 빌드는 정식 출시본이 아니다. 다음 항목을 확인하기 위한 평가용 버전이다.

- 처음 10분 동안 목표와 조작 흐름을 이해할 수 있는지
- 보고서 문장과 대응 선택지를 읽고 구분할 수 있는지
- 주요 UI의 가독성과 조작 피드백이 적절한지
- 화면 전환, 텍스트 출력, 효과음과 BGM이 불편하지 않은지
- 저장 및 불러오기 과정에서 오류가 발생하지 않는지

콘텐츠 분량, 밸런스, 문구와 연출은 최종 상태가 아니며 이후 변경될 수 있다.

## 2. 빌드 전 Git 상태 확인

```powershell
git status
```

다음을 확인한다.

- 의도하지 않은 수정·삭제·미추적 파일이 없는가
- 이번 알파에 포함할 변경이 모두 커밋되어 있는가
- 사용 중인 `.ogg`, `.mp3`, `.png` 원본과 해당 `.import` 파일이 함께 준비되어 있는가
- 개인용 저장 파일, 임시 파일, 로그 또는 배포 결과물이 포함되지 않았는가
- `builds/`와 배포용 `*.zip`이 Git 추적 대상이 아닌가

필요하면 빌드 직전 커밋을 남긴다.

```powershell
git add <이번 알파에 포함할 파일>
git commit -m "Prepare alpha test build"
```

작업 중인 다른 변경을 무조건 함께 커밋하지 않는다.

권장 태그 예시:

```powershell
git tag alpha-0.1.0
```

## 3. 빌드 이름과 폴더

권장 이름:

- `AMB_alpha_0.1.0_windows`
- 수정 빌드: `AMB_alpha_0.1.1_windows`

프로젝트 루트의 `builds/` 아래에 버전별 폴더를 사용한다.

```text
builds/
└─ AMB_alpha_0.1.0_windows/
   ├─ AnomalyManagementBranch.exe
   ├─ AnomalyManagementBranch.pck
   ├─ ALPHA_TESTER_README.txt
   └─ ALPHA_KNOWN_ISSUES.txt
```

기존 Windows Export Preset을 사용해 위 폴더로 내보낸다. Export Templates 설치 및 preset 확인은 앞선 단계에서 완료된 것으로 간주하며, 이번 문서에서는 반복하지 않는다.

내보내기 방식에 따라 `.pck`가 실행 파일에 포함될 수 있다. 분리된 `.pck`가 생성되었다면 반드시 `.exe`와 같은 폴더에 둔다.

## 4. 테스터 전달 패키지

전달할 최상위 압축 파일 예시:

```text
AMB_alpha_0.1.0_windows.zip
└─ AMB_alpha_0.1.0_windows/
   ├─ AnomalyManagementBranch.exe
   ├─ AnomalyManagementBranch.pck
   ├─ ALPHA_TESTER_README.txt
   └─ ALPHA_KNOWN_ISSUES.txt
```

- 이 저장소의 `ALPHA_TESTER_README.md`와 `ALPHA_KNOWN_ISSUES.md`를 배포본에서는 읽기 쉬운 `.txt` 또는 그대로의 `.md`로 동봉한다.
- `.exe`만 단독으로 전달하지 않는다. 내보내기 결과로 생성된 동반 파일과 폴더 구조를 유지한다.
- 테스터에게 압축 전체를 먼저 해제한 뒤 실행하도록 안내한다.
- 테스트 대상자로 지정된 사람에게만 전달한다.

## 5. 배포 전 직접 확인 체크리스트

- [ ] 압축 전 빌드 폴더에서 게임이 실행되는가
- [ ] 새 게임 또는 이어하기로 브리핑 화면에 진입할 수 있는가
- [ ] 브리핑에서 근무 화면으로 진입할 수 있는가
- [ ] 보고서 목록과 상세 내용이 표시되는가
- [ ] 보고서 본문 타자 출력과 스킵 입력이 작동하는가
- [ ] 선택지를 바꾸어 선택할 수 있는가
- [ ] 대응 확정 후 처리 완료와 확정된 대응 절차가 표시되는가
- [ ] 하루 종료 후 다음 날 브리핑으로 이동하는가
- [ ] 현재 관리 중인 이상현상 문서를 열람할 수 있는가
- [ ] 기록 보관실과 설정 화면에 진입하고 메인 메뉴로 돌아올 수 있는가
- [ ] 전체 음량, 화면 모드, 글자 크기 설정이 기존처럼 작동하는가
- [ ] 게임을 종료하고 다시 실행했을 때 현재 진행 저장을 불러올 수 있는가
- [ ] GOOD 또는 BAD 엔딩 중 테스트 가능한 경로가 정상 진입하는가
- [ ] 같은 BGM이 화면 이동 때 중복 재생되거나 처음부터 재시작하지 않는가
- [ ] hover, click, confirm, danger, report open 효과음이 중복 재생되지 않는가
- [ ] 문서·보고서 선택 때 패널 전체가 깜빡이지 않는가
- [ ] 내부 식별자나 상태값이 UI에 표시되지 않는가
- [ ] 압축 해제한 별도 폴더에서도 실행되는가

## 6. 저장 데이터 주의사항

게임은 다음 파일을 Godot의 `user://` 위치에 저장한다.

- `current_run_save.json`
- `settings_save.json`
- `archive_save.json`

Windows의 일반적인 위치는 `%APPDATA%\Godot\app_userdata\AnomalyManagementBranch\`이다. 환경에 따라 실제 위치는 달라질 수 있다.

- 알파 버전 간 저장 파일 호환은 보장하지 않는다.
- 저장/불러오기 문제를 재현할 때는 기존 저장 파일의 유무와 이전 빌드 버전을 함께 기록한다.
- 초기 상태 재현이 필요하면 먼저 저장 파일을 별도 백업한 뒤 사용자에게 삭제 여부를 확인한다.

## 7. 테스터에게 받을 피드백

- 처음 10분 동안 무엇을 해야 하는지 이해했는가
- 보고서의 흐름과 선택지 차이를 이해할 수 있었는가
- UI 글자와 문서가 읽기 편했는가
- 사운드나 텍스트 출력이 거슬리지 않았는가
- 하루 진행과 기록 보관실의 역할을 이해했는가
- 공포 분위기가 과하거나 지나치게 약하지 않았는가
- 다시 플레이하고 싶은 이유 또는 중단하고 싶은 이유는 무엇인가
- 막힘, 오류, 이해하기 어려운 문장이 있었는가

피드백은 이미 별도로 제공된 기존 테스트 기록지 형식에 따라 받는다. 이 저장소에 새 테스트 기록지를 만들지 않는다.

## 8. 제한 배포 주의사항

- 알파 버전에는 오류와 미완성 요소가 있을 수 있음을 미리 알린다.
- 무단 재배포를 금지하고 지정된 테스터에게만 공유한다.
- 스크린샷과 영상의 외부 공유 허용 여부는 배포 전에 결정한다.
- TODO: 스크린샷/영상 공유 허용 범위 확정 필요.
